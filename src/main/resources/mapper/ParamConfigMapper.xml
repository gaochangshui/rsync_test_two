<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trechina.planocycle.mapper.ParamConfigMapper">
  <select id="selectParamConfig" resultType="com.trechina.planocycle.entity.vo.ParamConfigVO">
    select config.item_cd item_cd,itam_value,COALESCE(config.item_name, param_mst.item_name) item_name from planocycle.param_config config
    inner join planocycle.param_mst param_mst on config.item_cd=param_mst.item_cd
    where type=#{type}
  </select>
</mapper>
