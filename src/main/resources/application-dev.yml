server:
  port: 8383
  tomcat:
    connection-timeout: 600000
#    context-path: /planoCycle
#    context-path: /cocacola-planocycle-api
spring:
  datasource:
    url: jdbc:postgresql://10.100.1.161:54332/PLANOCYCLE?characterEncoding=UTF-8&useSSL=false
    username: admin
    password: admin1234
    driver-class-name: org.postgresql.Driver
    hikari:
      # 最小空闲连接数量
      minimum-idle: 10
      # 连接池最大连接数，默认是10
      maximum-pool-size: 30
      # 此属性控制从池返回的连接的默认自动提交行为,默认值：true
      auto-commit: true
      # 空闲连接存活最大时间，默认600000（10分钟）
      idle-timeout: 30000
      pool-name: DatebookHikariCP
      # 此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
      max-lifetime: 1800000
      # 数据库连接超时时间,默认30秒，即30000
      connection-timeout: 60000
      connection-test-query: SELECT 1
  servlet:
    multipart:
      max-file-size: 30MB
  jackson:
    time-zone: Asia/Tokyo
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  file:
#    path: /
    path: D:\files
# 环境变量
SystemPath: http://10.100.1.163:8102/planocycleweb/
projectIds: nothing
bucketNames: nothing
ptsDownPath: http://10.100.1.163:8102/planocycleweb/
productDownPath: D:\files\
